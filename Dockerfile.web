FROM node:20-alpine

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies and build Web
RUN cd apps/web && npm install && npx next build

EXPOSE 3000

CMD ["sh", "-c", "cd apps/web && npx next start -p 3000"]
